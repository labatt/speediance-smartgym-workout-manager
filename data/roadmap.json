{
  "project": "Speediance Workout Manager — build status",
  "updated": "2026-08-31",
  "statusLegend": {
    "working": "Being worked on right now",
    "queued": "Accepted, not started yet",
    "blocked": "Waiting on something external",
    "future": "Idea for later, not committed",
    "done": "Finished and verified"
  },
  "items": [
    {
      "id": 1,
      "title": "Build the /roadmap build-status page",
      "summary": "A dependency-free page at /roadmap, backed by data/roadmap.json, that renders one table per status plus in-flight section tables. The living record of work on this project.",
      "affects": "data/roadmap.json, templates/roadmap.html, app.py (/roadmap and /roadmap.json routes)",
      "status": "done",
      "notes": "Exists so work is tracked in one honest place before anything depends on it — storage is a single JSON file the assistant edits directly (no DB, no admin UI, no auth, by design). Kept dependency-free on purpose: no CDN, no build step, so it still opens in five years. Verified the /roadmap route returns 200 and /roadmap.json parses. Observed conflict I did NOT silently resolve: the pasted rules 3-4 (done newest-first, everything else oldest-first as a queue) contradict the follow-up 'order everything newest at the top' — I implemented rules 2-4 as written and flagged it for a ruling rather than quietly picking one. Ruling (2026-08-30): user chose to keep rules 2-4 as the default; header-click sort still overrides per-table (rule 5). Resolved with no code change."
    },
    {
      "id": 2,
      "title": "Wellness Project exercise backfill",
      "summary": "Flask app finds empty Wellness Project (Health Connect) strength workouts and backfills them from the matching Speediance session's exercises, via an OAuth MCP client. Scheduled daily scan plus a manual button.",
      "affects": "wellness_client.py, reconcile.py, app.py (/wp/* routes), templates/wp_reconcile.html, tests/",
      "status": "done",
      "notes": "Design spec + 9-task TDD plan written and committed (docs/superpowers/specs/2026-08-30-wellness-project-backfill-design.md, docs/superpowers/plans/2026-08-30-wellness-project-backfill.md). Execution: subagent-driven development. Key design finding: Wellness Project exposes no REST API and no API key — its MCP endpoint IS the API, gated by OAuth (authorization_code + PKCE + refresh_token; no machine grant), so the app must connect once in a browser then refresh silently. The date+calories match key was exact across the live 90-day window. Built 2026-08-30 via 9-task subagent-driven development, all tasks reviewed clean and merged to main; automated suite 207 green (2 pre-existing unrelated e2e failures). The independent review gate caught and fixed two real bugs the plan itself had baked in: a CSRF state-expiry bypass (a stale OAuth `state` was still accepted) and a silent SSE data-loss (the MCP parser returned the first `data:` event, so a legal notification-before-result stream yielded an empty result). Two controller rulings: a dedup guard so one Speediance session can't fill two workouts, and a provenance note set directly rather than appending to the Health-Connect note. LIVE SMOKE TEST PASSED 2026-08-31: OAuth connect succeeded end-to-end (token stored 0o600); a real backfill filled Aug-29 'Miami Pull' (WP 696827) with its 5 exercises + provenance note and swept the other empty strength sessions, while the @ Gym and rowing/cardio entries were left untouched — the empty-only + dedup guards held. Observed surprise worth recording: Wellness Project's resolver RENAMES exercises to its own library (e.g. 'Barbell Bent Over Row' -> 'Row [Machine]', 'Seated Barbell Lat Pulldown' -> 'Close Grip Lat Pulldown'); loads and reps are exact, only the names/equipment tags remap. A 'keep literal name' flag is available if verbatim names are wanted later."
    },
    {
      "id": 3,
      "title": "Move Wellness Project backfill into Settings",
      "summary": "Retire the standalone /wp/reconcile page and surface Connect + Backfill directly in the Settings page's Wellness Project card, with a spinner while a backfill runs.",
      "affects": "app.py (settings route passes wp_connected; wp_callback + /wp/reconcile redirect to /settings), templates/settings.html, templates/wp_reconcile.html (removed), tests/test_wp_routes.py",
      "status": "done",
      "notes": "User found the separate /wp/reconcile page a 'weird empty page' and asked for the controls to live in Settings, plus a spinner during the run. Connect/Backfill move into the existing Wellness Project card; /wp/reconcile and the OAuth callback now redirect to /settings so old links and the redirect_uri still resolve. Done + verified live 2026-08-31: /settings shows Connect when disconnected and a Backfill-now button with an animated spinner + live results panel when connected; /wp/reconcile returns 302→/settings; the OAuth callback lands on /settings; wp_reconcile.html deleted; suite 208 green (2 known-unrelated e2e failures). Results panel and all dynamic text use textContent (no innerHTML), honoring the project XSS rule."
    }
  ]
}
